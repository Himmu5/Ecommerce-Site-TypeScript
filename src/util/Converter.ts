export default function convertImageUrl(originalUrl: string): string {
    // DummyJSON already provides complete, working image URLs
    // No conversion needed - return the original URL
    return originalUrl;
}
